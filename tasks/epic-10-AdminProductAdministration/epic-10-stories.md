# Epic 10 – Stories (Acceptance)

| # | Story | Acceptance Criteria (landed) | Real Reference |
|---|-------|------------------------|--------------------------|
| **10.1** | **ADMIN role in token + ADR 0011** — the token carries the role, the principal exposes the authority, auth bodies expose `role`; legacy tokens don't break. | • **ADR 0011** registered in the same commit (decisions D1–D4 from overview: role via env list, legacy compat, occupation of `ROLE_ADMIN` slot + consequence on actuator tiers, block semantics, no revocation in v1 + triggers). <br>• `AdminProperties` (new, `infra/config/properties/`): `app.admin-emails` (list; env `APP_ADMIN_EMAILS` comma-separated; empty = no admin; emails normalized trim/lowercase). <br>• `TokenPort.generateToken(email)` → `generateToken(email, role)`: claim `role` **only in the access token** (refresh unchanged — minimal surface); `JwtTokenProvider` emits the claim. <br>• `JwtAuthenticationFilter`: claim → `SimpleGrantedAuthority` `ROLE_ADMIN`/`ROLE_USER`; **absent claim → `ROLE_USER`** (legacy compat, tested). <br>• `UserService`: `AuthResult` gains `role`; login/register/refresh compute role from env list (not DB); `me(email)` also returns role. <br>• Additive DTOs: `AuthResponse` + `UserResponse` gain `role` (`"USER"\|"ADMIN"`) — OpenAPI updated (rest of body unchanged: dual-write ADR 0010 intact). <br>• **Tests for this story:** claim present in generated token (provider unit); filter → correct authority in 3 cases (admin/legacy/absent); `AuthResult.role` in 3 flows; `/me` with role; **bootstrap IT**: register with email from list → login body `role=ADMIN` + `/me` `role=ADMIN`; register outside list → `USER`. <br>• **Living spec:** new requirements in Auth component (gated package `infra/security`): role claim from env list; legacy token without claim → USER; `/me` returns role — **all traced** in this story's tests. `check-living-spec` PASS (Auth ≥ 100%). <br>• `./mvnw verify` + bash gates PASS. **Commit 1** of epic. | `TokenPort`/`JwtTokenAdapter`/`JwtTokenProvider`/`JwtAuthenticationFilter` (`infra/security`), `UserService`, `dto/auth/...` |
| **10.2** | **Block/unblock + user listing** — operator lists users, blocks and unblocks; blocked loses login and refresh (403). | • `User` record gains `boolean blocked` (after `name` — record order documented); Mongo entity mapper: **absent → `false`** (expand-only; **no V-migration** — `V*` are indexes/collections); `UserEntityTest` extended (mapping both ways). <br>• `UserRepositoryPort` + Mongo impl: `findPage(cursor, limit)` and `findPageByEmailPrefix(prefix, cursor, limit)` (stable sort `createdAt desc + id`; prefix **regex-escaped** — `q` containing `$`/`.` doesn't break) + `setBlocked(id, blocked)`. <br>• **New packages** (D3): `core/ports/incoming/admin/` (ports `AdminListUsers`, `AdminBlockUser`, `AdminUnblockUser`), impls in `core/service/` (prefix `Admin`), `AdminController` in `infra/adapter/input/rest/admin/` — thin: extracts caller identity (email + role) from principal and delegates; domain exceptions → existing handler. <br>• **`GET /api/v1/admin/users`**: items `{userId, email, name, role, blocked, createdAt}` — `role` = **live truth from env list** (not the claim); `limit`/`cursor` in urls list style (same default/cap; `nextCursor`/`hasMore` in DTO); `q` optional = email prefix. <br>• **`POST /api/v1/admin/users/{userId}/block`** and **`/unblock`**: idempotent, empty body, **204**; 404 non-existent user; **self-block → 400** (rejected: without logged-in UI the admin couldn't unblock themselves — recovery would become Mongo+curl). <br>• **Block enforce:** `UserService.login` and `refreshToken` — validate credential **first** (invalid → 401 unchanged), then `blocked` → `ForbiddenException("Account blocked.")` → **403**. AUTH throttle (before use case) unchanged: 429 still consumes bucket before 403. <br>• **ADMIN enforce:** every admin use case checks caller role ≠ ADMIN → `ForbiddenException` (403); anonymous → 401 via chain. <br>• `SecurityConfig`: explicit matcher `.requestMatchers("/api/v1/admin/...` (line truncated) | `User`/`UserEntity` + mapper, `UserRepositoryPort`/Mongo, new admin packages, `UserService.login`/`refreshToken`, `SecurityConfig` |
| **10.3** | **Read-only inspection: links by user + lookup by code** — operator opens any user's wallet (including archived) and finds any short code. | • **`GET /api/v1/admin/users/{userId}/urls`**: same contract as owner list (`LinkListResponse`: `items` of `ShortUrlResponse` + `nextCursor` + `hasMore`), **including archived** (`deletedAt` visible — field already in `ShortUrlResponse`); 404 non-existent user; cursor in owner list style (same stability). <br>• **`GET /api/v1/admin/urls?code=`**: resolves the code **globally** (any owner) — **the short code IS the document id** (verified: `GET /{id}` uses `findById` directly) → reuses existing port, **no new repository method**; response = `ShortUrlResponse` + `ownerUserId` + `ownerEmail`; `ownerEmail` **nullable** (user doc missing — documented in OpenAPI and tested); 404 unknown code. <br>• Read-only: no write in this story; ADMIN enforce on both use cases (D3). <br>• **Tests for this story:** urls by userId — correct items + **archived item shows `deletedAt`** + pagination; 404 user; lookup 200 with correct `ownerUserId`/`ownerEmail`; lookup 404 unknown code; lookup with deleted user (via `UserRepositoryPort.deleteById` in setup) → `ownerUserId` present, `ownerEmail` null. <br>• `./mvnw verify` + gates PASS. **Commit 3.** | `LinkQueryPort` (findById / list by user — already exist), `ShortUrlResponse`/`LinkListResponse`, `GetLinkUseCaseImpl` (404 via `UrlNotFoundException`) |
| **10.4** | **Force archive + write path block check** — operator archives link of any owner; blocked account cannot write (reads yes — v1). | • **`DELETE /api/v1/admin/urls/{id}`**: force archive — **same `deletedAt` semantics as owner archive** (`ArchiveLinkUseCase`) + **`urlCachePort.evict(id)`** (pattern in `UpdateLinkUseCaseImpl`); idempotent (**204** on already archived link); 404 non-existent link; ADMIN enforce. After: `GET /{id}` (public redirect) → 404 and owner keeps seeing item with `deletedAt` in their list. <br>• **Write path block check** (D4): `ShortenUrlUseCase` — when request carries session (Bearer **or** cookie) and user is blocked → `ForbiddenException` → **403 before shorten** (nothing written, nothing counted in quota); lookup only occurs **when there is a session** (common anonymous path doesn't change; documented limit: block is by account, not by IP — anonymous shorten continues). <br>• **No revocation in v1** (D4): blocked user's access JWT remains valid for reads until expiry — accepted and documented; denylist follow-up registered in **commit body** (and ADR 0011). <br>• **Tests for this story:** force archive → `GET /{id}` 404 + owner list shows `deletedAt` + **second force archive → 204** (idempotent) + 404 non-existent link; blocked with valid token (issued **before** block) → `POST /urls` **403** (and URL doesn't exist after — lookup by code 404) + `GET /api/v1/urls` **200** (reads, doesn't write); `POST /urls` **anonymous → 200** even for blocked email (documented limit). <br>• `./mvnw verify` + gates + frozen-metrics PASS (zero new meters). **Commit 4.** | `ArchiveLinkUseCase` (`deletedAt` semantics), `urlCachePort.evict` (pattern), `ShortenUrlUseCase`/`UrlShortenerService` (optional session already comes from controller), `ForbiddenException` |
| **10.5** | **Contract and final gates** — complete OpenAPI, CHANGELOG, AGENTS.md matrix, epic gates and live proof pasted in DoD. | • **OpenAPI** (`/v3/api-docs`): all 6 admin endpoints with `@Operation` (summary/description, params `limit`/`cursor`/`q`/`code`, responses 200/204/400/401/403/404); `role` documented in login/register/refresh/`/me` responses; **403 "Account blocked."** and **self-block 400** semantics written; `ownerEmail` nullable in lookup. The SPA's `docs/api-contract.md` is captured from here — **without this their doc is born stale** (ADR 0010 R1). <br>• **CHANGELOG** `[Unreleased]` → `### Added`: `role` (claim + bodies, additive); `blocked` + block/unblock; `GET /api/v1/admin/users` (+`q` prefix); `GET /api/v1/admin/users/{userId}/urls` (includes archived); `GET /api/v1/admin/urls?code=`; `DELETE /api/v1/admin/urls/{id}` (force archive); write path block check (403). <br>• **AGENTS.md**: epic item in debt/status matrix (EP10) + denylist/revocation follow-up listed (not ghost debt — has owner and trigger); `check-doc-sync` PASS. <br>• **Epic gates:** `./mvnw verify` complete (18 contract tests, mapped in `epic-10-testing.md`); bash gates (`check-boundaries`, `check-doc-sync`, `check-metrics-frozen`, `check-living-spec` + `--self-test`); ArchUnit (admin use cases in application layer — test already required in story 10.2); living-spec: Auth ≥ 100% traced, Admin component **still non-gated** (100% Admin story comes later, like any new component). <br>• **Live proof** (rule zero — pasted in DoD, secrets redacted): boot with `APP_ADMIN_EMAILS=<admin@…>`; admin register → login (body `role=ADMIN`) → `/me` (role ADMIN); victim register; **block** victim (204) → victim login **403** "Account blocked." → **unblock** (204) → login 200; victim shorten → **lookup by code** (correct ownerEmail) → **force archive** (204) → `GET /{id}` **404** → victim owner list shows `deletedAt`. All outputs with status + relevant headers. <br>• `git log --oneline` of the 5 commits pasted in DoD. | `@Operation`/`@ApiResponse` (existing controllers pattern), `CHANGELOG.md` (keep-a-changelog), `AGENTS.md` (matrix), `scripts/check-*.sh` |

---

**Quick Traceability:**

| Story | Reference Doc | Key Aspect |
|-------|----------------|---------------|
| 10.1 | ADR 0011 + `TokenPort`/`JwtTokenProvider`/filter + auth DTOs | Role in token + legacy compat |
| 10.2 | `User`(+`blocked`) + `UserRepositoryPort` + `AdminController` (users) | Block/unblock + listing |
| 10.3 | `AdminController` (urls by user, lookup by code) | Read-only inspection |
| 10.4 | `AdminController` (force archive) + write path | Forced mutation + write check |
| 10.5 | OpenAPI + CHANGELOG + AGENTS.md + DoD | Contract + gates + live proof |

---

*Execute in order 10.1 → 10.5 (`epic-10-technical-tasks.md`); each commit green on its own; evidence in `epic-10-dod.md`.*