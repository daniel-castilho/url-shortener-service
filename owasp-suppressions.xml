<?xml version="1.0" encoding="UTF-8"?>
<!--
  URL Shortener Service — OWASP Dependency-Check suppressions (Epic 2 story 2.5).

  RULE (AGENTS.md Rule 9 / dargent E13-S1 policy): every suppression carries a rationale
  comment + a review date. A suppression without both is a defect. Suppress the FINDING only
  when it is a false positive for this codebase (never because it is inconvenient); a real
  vulnerability gets a dependency bump, not a line here.

  Current state: intentionally EMPTY — the dependency set has no known CVSS >= 7 findings
  (verified 2026-09-10, `./mvnw dependency-check:check` green).
-->
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
</suppressions>